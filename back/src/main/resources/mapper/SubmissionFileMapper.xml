<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ast.back.modules.submission.persistence.mapper.SubmissionFileMapper">

    <select id="selectBySubmissionIds" resultType="com.ast.back.modules.submission.persistence.entity.SubmissionFile">
        select id, submission_id, filename, storage_path, sha256, bytes, parse_status, parse_error
        from submission_file
        where submission_id in
        <foreach collection="submissionIds" item="submissionId" open="(" separator="," close=")">
            #{submissionId}
        </foreach>
        order by submission_id asc, id asc
    </select>
</mapper>