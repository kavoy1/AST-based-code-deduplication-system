spring:
  datasource:
    # NOTE: Don't commit real passwords into git. Prefer environment variables.
    url: ${DB_URL:jdbc:mysql://localhost:3306/sch1_v2?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:395800}
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    # Optional: used by email verification code.
    host: ${MAIL_HOST:smtp.qq.com}
    username: ${MAIL_USERNAME:2769808840@qq.com}
    password: ${MAIL_PASSWORD:jhwllrdufocyddbb}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

# App-level configs (extend later)
app:
  ai:
    enabled: ${AI_ENABLED:false}
    provider: ${AI_PROVIDER:QWEN}
    base-url: ${AI_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
    dashscope-api-key: ${DASHSCOPE_API_KEY:}
    model: ${QWEN_MODEL:qwen-plus}
    timeout-ms: ${AI_TIMEOUT_MS:8000}
    prompt-version: ${AI_PROMPT_VERSION:QWEN_EXPLAIN_V1}
  storage:
    root-dir: ${STORAGE_ROOT:uploads}
    max-files: ${UPLOAD_MAX_FILES:20}
    max-file-bytes: ${UPLOAD_MAX_FILE_BYTES:2097152}
  plagiarism:
    executor:
      core-pool-size: ${PLAGIARISM_EXECUTOR_CORE_POOL_SIZE:2}
      max-pool-size: ${PLAGIARISM_EXECUTOR_MAX_POOL_SIZE:4}
      queue-capacity: ${PLAGIARISM_EXECUTOR_QUEUE_CAPACITY:20}